# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files (includes .env.local, .env.example is negated below if needed)
.env*
!.env.example
# timestamped rebuild backups: .env.local.backup-YYYYMMDD-HHMMSS

# Runtime reservation fallback files may contain guest PII.
# Keep sanitised fixtures in explicit fixture directories trackable.
data/gifts/*reservations*.json
!data/gifts/**/fixtures/*.json

# vercel
.vercel

# typescript
*.tsbuildinfo

# Audio original pesado (usar versão -web.mp3 no tema)
**/bg-music.mp3
public/audio/if-i-aint-got-you.mp3

# IDE
.cursor/
