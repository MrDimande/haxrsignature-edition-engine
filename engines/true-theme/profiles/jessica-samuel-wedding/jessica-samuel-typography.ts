/** Tipografia editorial — casamento black-tie Jessica & Samuel */
export const jsType = {
  micro:
    "font-display text-[10px] sm:text-[11px] font-light tracking-[0.38em] uppercase",
  eyebrow:
    "font-display italic text-xs sm:text-sm font-light tracking-[0.22em]",
  sectionLabel:
    "font-display text-[10px] sm:text-[11px] font-medium tracking-[0.32em] uppercase",
  sectionTitle:
    "font-display text-3xl sm:text-4xl md:text-[2.85rem] font-light tracking-[0.04em] leading-[1.1]",
  heroNames:
    "font-display text-[2.35rem] sm:text-5xl md:text-6xl lg:text-[3.75rem] font-light leading-[1.08] tracking-[0.02em]",
  heroAmpersand:
    "font-display italic text-2xl sm:text-3xl md:text-4xl font-light tracking-[0.12em]",
  heroMeta:
    "font-body text-[11px] sm:text-xs font-light tracking-[0.28em] uppercase",
  verse:
    "font-display italic text-lg sm:text-xl md:text-2xl font-light leading-relaxed tracking-[0.01em]",
  verseRef:
    "font-body text-[10px] sm:text-[11px] font-light tracking-[0.24em] uppercase",
  body:
    "font-body text-sm sm:text-base font-light leading-relaxed tracking-[0.01em]",
  bodyPoetic:
    "font-display italic text-base sm:text-lg font-light leading-relaxed",
  monogram:
    "font-display text-xs sm:text-sm font-normal tracking-[0.4em] uppercase",
} as const;
